# yaml-language-server: $schema=https://vscodeedu.com/assets/schema/course-v2.schema.json
id: lessonpart1
title: Take Home Lesson
duration: 5
content: |+
  ### 🧠 Concept Summary

  **print()**

  * Displays a message to the screen.

  ```
  print("Hello, world!")
  ```

  **input()**

  * Asks the user to type something.

  ```
  name = input("What is your name? ")
  ```

  **Comments**

  * Notes in your code for humans to read.

  ```
  # This line says hello
  print("Hello!")
  ```

  **String Concatenation**

  * Combining strings using `+`.

  ```
  print("Hello, " + name)
  ```

  **String Methods**

  * Functions you can use on strings.

  ```
  print(name.upper())
  print(name.lower())
  ```

  **Type Checking**

  * Use `type()` to find out what kind of data something is.

  ```
  print(type(name))
  ```

  ***

  ### 🧪 Code Examples (Quick Start)

  ```
  # Say hello and ask a question
  print("Welcome to class!")
  name = input("What's your name? ")
  print("Nice to meet you, " + name + "!")

  # Try string methods
  print(name.upper())
  print(name.lower())
  print("Your name has", len(name), "letters.")
  ```

  ***

  ### 🏠 Take-Home Challenge: My Intro Script

  Create a script called `my_intro_script.py` that:

  * Asks the user for their name, favorite food, and a number
  * Prints a few fun sentences using the answers
  * Uses string concatenation and at least one string method
  * Includes 2 comments to explain your code

  **Example Output:**

  ```
  What is your name? Travis
  What's your favorite food? tacos
  Pick a number: 3

  Hello Travis!
  Wow, 3 tacos sounds great!
  TRAVIS really likes tacos.
  ```

  Submit your file to Google Classroom when finished.

